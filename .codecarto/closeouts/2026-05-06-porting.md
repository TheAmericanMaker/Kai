# Closeout — 2026-05-06 — porting

## Summary

Sixth implementing session. Drove the porting phase— synthesised architecture-map.md + behavioral-contracts.md + protocols-and-state.md + mechanical-defects.md + semantic-defects.md into `findings/porting/reverse-engineering-bundle.md`. Triaged 34 defects (16 mechanical + 18 semantic) into— **8 fix-before-porting**, **13 port-differently**, **10 leave-behind**. Closed proto-CF3 (no _schema_version) by surfacing as a porting recommendation. Routed two new carry_forward items to reimplementation-spec for strategic-alignment decisions (HIGH-security cluster + Android-only-sandbox asymmetry).

## Files Touched

- **Added:** `.codecarto/findings/porting/reverse-engineering-bundle.md`; `.codecarto/closeouts/2026-05-06-porting.md` (this file).
- **Modified:** `.codecarto/workflow/status.yaml` (porting → complete; current_phase → reimplementation-spec); `.codecarto/THREAD_LOG.md`.

## Decisions Beyond Prompt

- **D-13** | Triaged the 34-defect total into a 3-bucket recommendation (fix / port differently / leave behind) at the porting layer rather than deferring all of it to reimplementation-spec. | Per the porting SKILL.md guidance ("sort features by porting importance" + "reference relevant defects in the feature contract table"), the porting bundle owns the triage call. The reimpl-spec consumes the triage and decides per-recommendation tier in the spec body.
- **D-14** | Routed the four-HIGH-security cluster forward as a single carry_forward (port-CF1) rather than four separate items. | They cluster around "secrets / privacy moving across the trust boundary" — the spec treatment is unified (e.g., a single "Security baseline" section) rather than four scattered fixes.

## Carry-Forward Routed

| ID | Target Phase | Description |
|---|---|---|
| port-CF1 | reimplementation-spec | Four HIGH-security cluster— explicit spec treatment. |
| port-CF2 | reimplementation-spec | Android-only-sandbox asymmetry decision. |

## Carry-Forward Closed

| ID (was) | From phase | Closed because |
|---|---|---|
| proto-CF3 | protocols | Surfaced in §Portability Hazards + §Defect Synthesis 5.3 as a fix-before-porting recommendation. |

## Next Session Pointer

Reimplementation-spec is the final pipeline phase. Per GUIDE.md "Strategic Alignment Hook"— the orchestrator must ASK the user before this phase—
1. **Language-agnostic** (default, any port to any stack) → use `templates/reimplementation-spec.md`.
2. **Opinionated** (target stack, project identity, scope cuts, named primitives pre-locked) → use `templates/reimplementation-spec-opinionated.md`.

Ask the user. Both port-CF1 and port-CF2 carry_forward items influence the answer (the security-cluster fixes are easier to spec for an opinionated target stack; the sandbox asymmetry is easier to re-affirm if the port is opinionated about sticking with Android-only).
